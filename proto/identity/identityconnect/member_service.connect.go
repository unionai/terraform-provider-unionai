// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: identity/member_service.proto

package identityconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	identity "github.com/unionai/terraform-provider-unionai/proto/identity"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// MemberServiceName is the fully-qualified name of the MemberService service.
	MemberServiceName = "cloudidl.identity.MemberService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// MemberServiceListMembersProcedure is the fully-qualified name of the MemberService's ListMembers
	// RPC.
	MemberServiceListMembersProcedure = "/cloudidl.identity.MemberService/ListMembers"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	memberServiceServiceDescriptor           = identity.File_identity_member_service_proto.Services().ByName("MemberService")
	memberServiceListMembersMethodDescriptor = memberServiceServiceDescriptor.Methods().ByName("ListMembers")
)

// MemberServiceClient is a client for the cloudidl.identity.MemberService service.
type MemberServiceClient interface {
	// Lists all members (apps and users) for the given organization.
	ListMembers(context.Context, *connect.Request[identity.ListMembersRequest]) (*connect.Response[identity.ListMembersResponse], error)
}

// NewMemberServiceClient constructs a client for the cloudidl.identity.MemberService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewMemberServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) MemberServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &memberServiceClient{
		listMembers: connect.NewClient[identity.ListMembersRequest, identity.ListMembersResponse](
			httpClient,
			baseURL+MemberServiceListMembersProcedure,
			connect.WithSchema(memberServiceListMembersMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// memberServiceClient implements MemberServiceClient.
type memberServiceClient struct {
	listMembers *connect.Client[identity.ListMembersRequest, identity.ListMembersResponse]
}

// ListMembers calls cloudidl.identity.MemberService.ListMembers.
func (c *memberServiceClient) ListMembers(ctx context.Context, req *connect.Request[identity.ListMembersRequest]) (*connect.Response[identity.ListMembersResponse], error) {
	return c.listMembers.CallUnary(ctx, req)
}

// MemberServiceHandler is an implementation of the cloudidl.identity.MemberService service.
type MemberServiceHandler interface {
	// Lists all members (apps and users) for the given organization.
	ListMembers(context.Context, *connect.Request[identity.ListMembersRequest]) (*connect.Response[identity.ListMembersResponse], error)
}

// NewMemberServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewMemberServiceHandler(svc MemberServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	memberServiceListMembersHandler := connect.NewUnaryHandler(
		MemberServiceListMembersProcedure,
		svc.ListMembers,
		connect.WithSchema(memberServiceListMembersMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/cloudidl.identity.MemberService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case MemberServiceListMembersProcedure:
			memberServiceListMembersHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedMemberServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedMemberServiceHandler struct{}

func (UnimplementedMemberServiceHandler) ListMembers(context.Context, *connect.Request[identity.ListMembersRequest]) (*connect.Response[identity.ListMembersResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("cloudidl.identity.MemberService.ListMembers is not implemented"))
}
